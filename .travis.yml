language: cpp
os: windows
before_install:
  - choco install microsoft-build-tools-2013
env:
  - MSBUILD_PATH="C:\Program Files (x86)\MSBuild\12.0\Bin"
script:
  - export PATH=$MSBUILD_PATH:$PATH
  - ./build.bat
  