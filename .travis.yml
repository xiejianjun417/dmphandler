language: cpp
os: windows
before_install:
  - choco install microsoft-build-tools-2013
env:
  - MSBUILD_PATH="c:\Program Files (x86)\MSBuild"
script:
  - export PATH=$MSBUILD_PATH:$PATH
  - ./build.bat
  